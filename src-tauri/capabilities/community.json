{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "community-capability",
  "description": "Capability for community features: profile, feed, lessons, streaks",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
